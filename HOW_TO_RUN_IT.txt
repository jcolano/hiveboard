  To run the E2E demo:
  1. Copy config.example.json to config.json and fill in your values
  2. uvicorn backend.app:app --port 8000
  # In another terminal:
  python examples/simulator.py --fast
  # Then open http://localhost:8000/dashboard
